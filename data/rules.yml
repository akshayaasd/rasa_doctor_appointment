version: "3.1"

rules:
- rule: Activate appointment form
  steps:
  - intent: book_appointment
  - action: appointment_form
  - active_loop: appointment_form

- rule: Submit appointment form and save data
  condition:
  - active_loop: appointment_form
  steps:
  - action: appointment_form
  - active_loop: null
  - action: action_save_appointment_to_db

